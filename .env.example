# Frontend Environment Variables
# Copy this to .env.local for local development

# Set to 'true' to use standalone local server instead of Supabase
VITE_USE_STANDALONE=true

# URL of your standalone server (default: http://localhost:3001)
VITE_STANDALONE_URL=http://localhost:3001
